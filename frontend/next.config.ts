import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['images.unsplash.com', 'source.unsplash.com', 'istockphoto.com', 'media.istockphoto.com'],
  }
};

export default nextConfig;
