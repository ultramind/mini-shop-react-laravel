import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['images.unsplash.com', 'source.unsplash.com', 'istockphoto.com', 'media.istockphoto.com', 'http://127.0.0.1:8000', 'http://localhost:8000'],
  }
};

export default nextConfig;
